[workspace]
members = [
    "server",
    "bench",
    "test-support",
    "core",
    "ui",
    "kv/*",
    "blob/*",
    "format/*",
    "scanner/*",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
