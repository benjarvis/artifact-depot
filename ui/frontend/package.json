{
  "name": "depot-ui",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "build:test": "vue-tsc --noEmit && vite build --config vite.config.test.ts",
    "preview": "vite preview",
    "test:e2e": "npx playwright test",
    "coverage:report": "npx nyc report",
    "test:gallery": "npx tsx e2e/generate-gallery.ts"
  },
  "dependencies": {
    "@vueuse/core": "^14.2.1",
    "pinia": "^3.0.4",
    "swagger-ui-dist": "^5.32.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "nyc": "^17.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-istanbul": "^6.0.0",
    "vue-tsc": "^2.2.0"
  }
}
